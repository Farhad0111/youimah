version: '3'

services:
  api:
    build: .
    ports:
      - "8033:8033"
    volumes:
      - ./videos:/app/videos
    environment:
      - VEO3_GEN_API_KEY=${VEO3_GEN_API_KEY}
      - GOOGLE_VEO3_API_KEY=${GOOGLE_VEO3_API_KEY}
    restart: unless-stopped
